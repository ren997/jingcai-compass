spring:
  application:
    name: jingcai-compass-backend
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  datasource:
    url: ${DB_URL:jdbc:postgresql://110.40.155.140:35432/jingcai_compass}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:2r6ZpEKEATdB8kis}
  data:
    redis:
      host: ${REDIS_HOST:110.40.155.140}
      port: ${REDIS_PORT:26739}
      password: ${REDIS_PASSWORD:JrSh6bfDxanXwm3c}
  flyway:
    enabled: true
    locations: classpath:db/migration
  jackson:
    time-zone: UTC

server:
  port: ${SERVER_PORT:8080}
