/** T602 前的最小后台入口。 */
export default function AdminHomePage() {
  return (
    <main className="admin-page">
      <section className="admin-card">
        <p className="eyebrow">Admin workspace</p>
        <h1>后台已就绪</h1>
        <p>同步运行、映射复核与预测管理页面将在后续任务中逐项接入。</p>
      </section>
    </main>
  );
}
